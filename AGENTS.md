# AGENTS.md

## 1. 目的と優先順位
- このリポジトリでは、仕様書駆動開発、テスト駆動開発、複数エージェント協働を標準とする。
- 本ファイルは、基本ゲート、禁止事項、成果物の標準置き場を定義する。詳細な役割は `.codex/agents/`、具体手順は `.agents/skills/` を正とする。
- 人間は、プロダクト判断、曖昧な要求の最終判断、安定版ブランチやリリースへの昇格承認を担う。
- 優先順位は、明示的なユーザー指示 > 最も近い `docs/specs/` > 親階層の `docs/specs/` > 最も近い `.codex/` > 親階層の `.codex/` > 最も近い `AGENTS.md` > 親階層の `AGENTS.md` とする。
- 指示が矛盾する場合は、矛盾点を示し、優先順位が高い指示に従う。

## 2. 標準成果物
既存プロジェクトに別の置き場や命名規約がある場合は既存規約を優先し、対応関係を引き継ぎ報告に明示する。

| 成果物 | 標準の置き場 |
| --- | --- |
| 承認済み仕様 | `docs/specs/overview.md`、`requirements.md`、`design.md`、`decision_log.md` または `docs/specs/decisions/` |
| 仕様草案 | `.codex/spec-drafts/<feature>.md` |
| 未確定仕様 | `.codex/open-issues/<feature>.md` |
| 作業メモ | `.codex/work-notes/<feature>.md` |
| 実装中の暫定判断 | `.codex/implementation_notes.md` または `.codex/work-notes/<feature>.md` |
| テスト計画、受け入れ条件対応表 | `.codex/test-plans/<feature>.md` |
| テストケース定義 | `.codex/test-cases/<feature>.md` |
| データ駆動テスト用ケース | `.codex/test-cases/<feature>.csv`、`.yaml`、`.json` など |
| 実行可能受け入れテスト | `tests/**` またはリポジトリ管理下の検証スクリプト |
| 追跡表 | `.codex/traceability/<feature>.yaml` |
| 検証報告 | `.codex/validation/<feature>.md`、`.codex/validation/<feature>.json` |
| 検証証跡 | `.codex/validation/artifacts/<feature>/<name>` など。原則 Git 管理しない |
| 実行成果物 | `outputs/`。原則 Git 管理しない |

- `docs/specs/` は現在有効な承認済み仕様だけを置く。草案、却下案、長い議論経緯は置かない。
- `README.md` は入口文書であり、要求や設計の正本にはしない。
- `AGENTS.md` には個別機能の受け入れ条件や長い議論経緯を書かない。

## 3. 主要用語
- `実装可能ブランチ`: `未確定仕様` がなく、受け入れ条件、テスト計画、テストケース定義、受け入れ条件対応表が揃い、`UNLOCK:IMPLEMENT` により実装へ進める `feature/*` ブランチ。
- `統合ブランチ`: `feature/*` の統合先。既存運用がなければ `develop`。
- `安定版ブランチ`: リリース基準。既存運用がなければ `main`。
- `検証ブランチ`: 仕様判断用の補助ブランチ。既定は `experiment/*`。
- `実験実装`: `検証ブランチ` の暫定実装。本実装や完了扱いにはしない。

## 4. 作業フェーズ
- 標準フェーズは、`仕様策定フェーズ`、`実装許可待ち`、`実装フェーズ`、`検証・整理フェーズ`、`統合判断フェーズ` とする。
- フェーズが曖昧な場合、メインエージェントは現在フェーズ、進めてよい作業、ブロック条件を整理してから進める。

### 4.1. 仕様策定フェーズ
- `spec-designer` の最新出力を前提に `test-designer` がテスト可能性を確認する。両者を単純並列完了扱いにしない。
- 仕様不備、期待値不明、境界条件不足、受け入れ条件のずれがあれば `spec-designer` へ差し戻し、収束するまで完了扱いにしない。
- `test-designer` が OK とし、`未確定仕様` と人間判断待ちが残らない場合、`spec-designer` が `docs/specs/` へ昇格する。
- 人間判断が必要な事項が残る場合は昇格せず、`.codex/open-issues/` を現在判断が必要な項目だけに保つ。
- このフェーズでは、本体コード、`tests/**`、依存定義、ロックファイル、環境設定、検証スクリプトは変更しない。ただし人間の明示指示、または `検証ブランチ` での必要最小限の変更は除く。

### 4.2. 実装許可待ち
- 承認済み仕様、受け入れ条件、テスト計画、テストケース定義、受け入れ条件対応表が揃った状態を実装許可待ちとする。
- `UNLOCK:IMPLEMENT` がない限り、本体コード、`tests/**`、依存定義、環境設定、検証スクリプトは変更しない。

### 4.3. 実装フェーズ
- 実装は、当該メッセージに `UNLOCK:IMPLEMENT` が明示された場合のみ開始する。
- `未確定仕様` が残る、またはテスト計画、テストケース定義、受け入れ条件対応表が不足する場合は `UNLOCK:IMPLEMENT` を受理しない。
- `test-designer`、`validator`、`implementer` を単純並列完了扱いにしない。順序は、受け入れテスト作成または更新、red 確認、実装、green 検証とする。
- `validator` がテスト不備や red 不成立を指摘した場合は `test-designer` へ差し戻す。仕様の未確定事項が見つかった場合は仕様策定フェーズへ戻す。
- `implementer` は受け入れテストを原則変更せず、本体コードを green にする。実装都合のテスト弱体化、仕様外実装、未合意の依存変更は差し戻す。
- `validator` が `必須修正` または `実装フェーズ内の推奨修正` を指摘した場合は、実装またはテスト担当へ差し戻す。

### 4.4. 検証・整理フェーズ
- `validator` は最小の関連検証から始め、必要に応じて範囲を広げる。
- `validator` は仕様、受け入れ条件、テスト、実装、README、設定の対応と、変更差分周辺の品質を確認する。
- 総合判断は `.codex/validation/<feature>.json` に記録し、標準検査スクリプトで PR 準備可否を確認する。
- 必須検証不足、未解決仕様、対応表と検証結果の不整合、`必須修正` 残存時は PR 準備可能と扱わない。

### 4.5. 統合判断フェーズ
- メインエージェントは、仕様対応、検証結果、未検証範囲、品質指摘、残リスク、人間判断が必要な事項を整理する。
- validator が PR 準備可能と判断した後、メインエージェントは `commit-feature` skill に従って検証済み差分だけを local commit する。
- push、下書き PR 作成、ready for review、レビュー依頼、merge、`統合ブランチ` への pull は人間の承認なしに行わない。

## 5. 権限と禁止事項
- 作業開始時に、標準導入済みか、既存運用の追加制約があるかを簡潔に共有する。
- 既存運用がある場合は、既存のソース構成、依存管理、実行方法、テスト方法、CI 運用、命名規約を尊重し、標準構成への寄せは明示合意なしに行わない。
- `UNLOCK:IMPLEMENT` がなくても、読取、レビュー、調査、設計提案、`docs/specs/`、`.codex/`、`.agents/skills/`、`AGENTS.md`、`.gitignore` の編集、状態確認、ブランチ作成・切替、`add`、`commit`、不要ブランチ整理は行ってよい。
- `UNLOCK:IMPLEMENT` がなくても、テスト計画、テストケース定義、受け入れ条件対応表の更新は行ってよい。ただしテストファイル編集、テスト実行、検証スクリプト実行は明示指示または実装許可がある場合に限る。
- 明示合意なしに、ツールチェーン移行、システム環境変更、プロジェクト外環境変更、共有環境や本番相当環境への反映を行わない。
- 秘密情報、認証情報、秘密鍵、トークン、機微な個人情報を表示、コミット、外部送信しない。
- 不可逆なデータ変更、外部サービスへの書き込み、大量削除、hard reset、強制クリーンアップ、履歴書き換えは、明示承認なしに行わない。
- 再生成可能なキャッシュや検証生ログは `.gitignore` で除外する。tracked ファイルを `git rm --cached` する場合や大きな削除は確認する。
- ソースコード、承認済み仕様、テストケース定義、軽量設定、依存定義、ロックファイルをキャッシュ扱いで ignore しない。

## 6. Docker と実行環境
- 通常は Docker を使わず、プロジェクトローカルの実行環境で解決する。
- OS、CUDA、システムライブラリ、複数サービス統合、再現可能な隔離環境などで必要な場合のみ Docker を検討する。
- 既存規約がなければ、通常は `ubuntu:24.04`、CUDA が必要なら Ubuntu 24.04 系の NVIDIA 公式 CUDA イメージを初期候補とする。
- Dockerfile、compose、GPU 設定を追加する場合は、必要性と前提条件を `docs/specs/`、`.codex/work-notes/`、または検証報告に記録する。
- 明示承認なしに、`--privileged`、広範な host mount、host 側の system package 変更、共有環境や本番相当環境への接続を行わない。

## 7. エージェント協働
- ユーザー対話、承認取得、sub-agent 起動、差し戻し制御、成果物統合、最終報告はメインエージェントが担う。
- 推奨役割は `spec-designer`、`test-designer`、`implementer`、`validator` とする。標準フェーズでは対応する専門エージェントを必須とする。
- 編集を伴う sub-agent は、可能なら `worker` として起動し、担当ファイル、責務、`write_scope`、他者変更を戻さないことを明示する。
- メインエージェントが直接編集してよいのは、機械的統合、衝突解消、標準導入や標準更新そのもの、またはユーザーが明示した例外に限る。
- メインエージェントの既定モデルは `gpt-5.4`、既定 `reasoning_effort` は `medium` とする。高リスクの仕様判断、広範囲レビュー、失敗原因調査、セキュリティや不可逆操作に関わる判断では、理由を明示して `gpt-5.5` の `high` を使ってよい。
- sub-agent 起動時は `model` と `reasoning_effort` を必ず明示する。省略すると親設定を継承するため、省略しない。
- 既定は、`spec-designer` は `gpt-5.4` `medium`、`test-designer` は `gpt-5.4` `medium`、軽量な観点出しのみなら `gpt-5.4-mini` `medium`、`implementer` は `gpt-5.4` `medium`、`validator` は `gpt-5.4` `high` とする。
- `spec-designer` と `implementer` は高リスク時に `high` を使ってよい。`validator` は高リスク時に `gpt-5.5` `high` を使ってよい。`xhigh` はユーザーが明示した場合、または非常に広範囲で高リスクな調査に限る。
- 同一フェーズ、同一 `feature/*`、同一責務の差し戻しでは既存 sub-agent を再利用する。再開できない場合に限り後任を起動する。
- 必要な sub-agent を起動できない場合、または上位ルールにより明示依頼なしでは起動できない場合は標準フェーズの作業を開始しない。返答は `multi-agent 標準運用で進めてください。必要な sub-agent を使ってください。` とする。
- 最終報告には使用した sub-agent を含める。必須 sub-agent が未使用の場合、その作業は完了扱いにしない。

## 8. Git / GitHub ワークフロー
- ソースコードと仕様書は原則 Git で管理する。既存運用がなければ `main` を安定版、`develop` を統合ブランチとする。
- 標準初期化そのものの Git 手順と完了条件は `skills/bootstrap-team-repo` に従う。
- `git fetch --prune origin` の標準タイミングは、新しい `feature/*` 作成前、`UNLOCK:IMPLEMENT` 前、push または下書き PR 作成前、ユーザーが明示した場合とする。
- fetch 後に remote tracking branch にローカル未反映の更新がある場合は、人間に状況と選択肢を提示し、許可された場合のみ `pull`、`merge`、`rebase` を行う。
- validator が PR 準備可能と判断した後の local commit はメインエージェントが行う。sub-agent は `git add`、`git commit`、push、下書き PR 作成を行わない。
- GitHub CLI、認証、ネットワーク権限、リモート設定が使えない場合は、push と PR 作成を代行せず、必要コマンド、PR タイトル、PR 本文、残課題を提示する。
- PR には、対応仕様、受け入れ条件、テストケース定義、検証結果、未検証範囲、品質指摘、残リスクを記載する。

## 9. 仕様、テスト、記録
- 承認済み仕様には、実装判断に必要な受け入れ条件を含める。
- `仕様草案` を `承認済み仕様` へ昇格する条件は、人間の明示承認、または `test-designer` が OK と判断し、`未確定仕様` と人間判断待ちが残っていないことである。
- 人間が判断した事項は、決定内容、理由、却下した主な選択肢、日付または文脈を `docs/specs/decision_log.md` または `docs/specs/decisions/` に記録する。
- `.codex/open-issues/` には現在判断が必要な未解決事項だけを残す。
- 公開挙動、データ形式、外部契約、セキュリティ、不可逆操作に影響する要求を黙って補完しない。
- テストは仕様と受け入れ条件から導く。既存方針がない限り外部から観測できる挙動を中心にする。
- `UNLOCK:IMPLEMENT` 後は、本体コード変更前に `実行可能受け入れテスト` を追加または更新し、妥当な red を確認する。
- セットアップ、使い方、コマンド、エントリポイント、公開インターフェイス、設定が変わる場合は `README.md` を更新する。

## 10. コミュニケーション
- 作業開始時に、理解した目標と最初の具体的な手順を共有する。
- 仕様策定フェーズでは、未解決の論点と実装ブロックの有無を明示する。
- ファイル編集前に、何をなぜ変更するかを共有する。
- 最終報告では、変更したファイル、実行した検証、未解決事項、追加判断が必要な点を簡潔にまとめる。

---

## python 追加指示


## 1. 目的
- このテンプレートは、Python プロジェクト全般に向けて `templates/base/AGENTS.md` を拡張する。
- テンプレート種別名は `python` に統一し、派生名は使わない。
- 先に基本テンプレートを適用し、その後にこの Python 固有の追加指示を適用する。

## 2. Python 基準
- Python を主要実装言語として使う。
- リポジトリにより厳しいスタイルが文書化されていない限り、PEP 8 に従う。
- コードコメント、識別子、例外メッセージ、ログ、CLI help などの実行時テキストは、仕様が別言語を要求しない限り英語で書く。
- チャット報告、仕様書、設計メモなど人間向け文書は、チームの作業言語で書いてよい。
- 広いスクリプト形式の実装より、小さく型付けされたテストしやすい関数を優先する。

## 3. 依存関係と環境方針
- `既存プロジェクト` では、仕様が明示的に変更しない限り、現在の依存管理ツールと実行環境を維持する。
- 新規または未標準化の Python プロジェクトでは、既定で `uv` を使う。
- `uv` を使う場合は次を優先する。
  - 依存変更: `uv add ...` または `uv remove ...`
  - 同期: `uv sync`
  - 実行: `uv run python ...`
  - テスト: `uv run pytest`
- プロジェクトがロックファイルを使う場合はバージョン管理に含める。
- パッケージをグローバルインストールしたり system Python を変更したりしない。

## 4. プロジェクト構成
- 既存のパッケージ構成、モジュール命名、エントリポイント規約を尊重する。
- 既存規約がない新規ファイルでは、Python モジュールとスクリプトに `snake_case` を使う。
- 設定はリポジトリの既存設定場所に置く。仕様上の理由なく新しい設定ディレクトリを導入しない。
- 明示的に必要な場合を除き、生成出力、キャッシュ、大きなローカルデータ、notebook 出力はバージョン管理に含めない。
- `__pycache__/`、`*.pyc`、`.pytest_cache/`、`.mypy_cache/`、`.ruff_cache/`、coverage 出力は `.gitignore` で除外し、作業ごとに削除承認を求めない。

## 5. TDD と検証
- テストは `docs/specs/` の受け入れ条件から導く。
- 既存テストフレームワークがない場合は `pytest` を優先する。
- pytest を使う場合、通常の検証コマンドは `uv run pytest` とする。
- red/green 確認、JUnit XML、検証報告の具体的な扱いは、合成後の `.codex/agents/test-designer.toml`、`.codex/agents/validator.toml`、および `run-feature-validation` スキルに従う。
- データ駆動テストを使う場合は、`.codex/test-cases/<feature>.csv`、`.yaml`、`.json` などを pytest から読み込み、テストケース定義との対応を維持する。
- 実装変更では、最も近い関連テストを先に実行する。
- テストが存在しない場合でも、少なくとも次を行う。
  - 変更したモジュールの import 確認
  - 適用できる場合は CLI `--help` または dry-run 確認
  - 実用的な場合は最小で安全な実行経路
- サービスコードでは、仕様に含まれる場合、設定解析、エラー処理、境界挙動の検証を含める。

## 6. 公開インターフェイス
- 有効な仕様が変更しない限り、公開 API、CLI フラグ、設定キー、環境変数、出力形式、シリアライズ済みデータ形式を維持する。
- 公開インターフェイスが変わる場合は、同じ機能スコープ内で `README.md`、関連 `docs/specs/`、テストを更新する。
- 承認済み変更に必要な場合を除き、公開モジュール構造の広範なリファクタリングは避ける。

## 7. Docker と Python 実行環境
- 通常の開発では Docker を使わず、プロジェクトローカルの `uv` 環境を優先する。
- OS バージョン、CUDA、システムライブラリ、統合依存などにより Docker が必要になった場合のみ、base の Docker 方針に従って検討する。
- Docker 内でも、既存プロジェクトで別方針がない限り Python の依存管理と実行は `uv` を使う。
- CUDA が必要な場合は、NVIDIA 公式の CUDA イメージのうち Ubuntu 24.04 系を初期検討候補とし、Python package だけでなく CUDA runtime、driver compatibility、GPU availability を検証観点に含める。
- Docker 内で pytest を使う場合も、通常の検証コマンドは `uv run pytest` とし、JUnit XML、検証報告、ログの出力先は `.codex/validation/` 方針に従う。
- パッケージのグローバルインストール、system Python の変更、host 側 system package の変更を前提にしない。
