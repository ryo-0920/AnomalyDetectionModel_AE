# 誤踏み判定モデル評価仕様書
## 1. 文書の目的
本書は、誤踏み判定モデルの評価条件、評価単位、判定基準、集計項目、出力対象を定義する仕様書である。
評価ロジックの決定に必要な前提、用語、データ定義、算出条件を本文中に明記する。
本書の目的は以下のとおりとする。
- 複数の誤踏み判定モデルを共通の条件で比較できるようにすること
- 事故データに対する検知性能と通常走行データに対する誤検知性能を同一の枠組みで評価できるようにすること
- データの定義と判定条件を明確にし、評価結果の解釈に差異が生じないようにすること
---
## 2. 評価対象
### 2.1 対象モデル
評価対象は以下の誤踏み判定モデルとする。
- Random Forest
- Time Series 系モデル
  - LSTM
  - GRU
  - TCN
  - Transformer
- Autoencoder
### 2.2 モデルの位置付け
各モデルの概要は以下のとおりとする。
| 項目             | Random Forest | Time Series 系モデル | Autoencoder   |
|------------------|--------------|----------------------|---------------|
| 学習の考え方     | 教師あり     | 教師あり             | 半教師あり    |
| 主な入力         | 短時間データ | 長時間時系列データ   | 通常走行データ |
| 判定の考え方     | 特徴量ベース | 時系列パターンベース | 再構成誤差ベース |
本評価では、モデルの学習方法や入力長の違いに関わらず、最終的に各時点を「異常」または「正常」とみなせる状態に正規化したうえで評価する。
---
## 3. 背景
誤踏み判定では、アクセル開度、車速、その他の車両状態量を含む多変量時系列データを扱う。
そのため、単一の状態量だけでなく、複数の状態量の組み合わせと時間変化を踏まえて異常性を評価する必要がある。
また、比較対象の各モデルは以下の点で前提が一致しない。
- 入力データの時間長が異なる
- 学習方式が異なる
- 学習に使用するデータ種別が異なる
さらに、通常走行データが事故データよりも大幅に多いというデータ不均衡が存在する。
このため、本評価では学習方法ではなく、評価時点における出力結果を共通ルールで比較することを重視する。
---
## 4. 評価の目的
本評価の目的は以下のとおりとする。
- 誤踏み判定に対して機械学習を適用した場合に、有効な検知性能が得られるかを確認する。
- 共通の基準により、各モデルの検知性能および誤検知性能を比較する。
- 各モデルについて、以下を整理する。
  - 検知しやすい条件
  - 検知しにくい条件
  - 誤検知しやすい条件
  - モデル固有の傾向
---
## 5. 用語定義
資料で共通の語句について定義する。
| 用語             | 定義                                                                                     |
|------------------|------------------------------------------------------------------------------------------|
| A1               | 誤踏み前兆を示す区間                                                                     |
| A2               | 誤踏みを示す区間                                                                         |
| A3               | 衝突を示す区間                                                                           |
| 事故区間時刻     | 事故区間を識別するための時刻情報                                                         |
| 初検知時刻       | 最初に異常と判定された時刻                                                               |
| 初検知区間       | 初検知時刻が属する事故区間                                                               |
| 初検知区間なし   | 初検知区間が定まらない状態。一度も異常を検知しない場合、または初検知時刻が A1、A2、A3 のいずれにも属さない場合をいう |
---
## 6. 評価の全体構成
評価は以下の 4 区分で構成する。
- 混同行列評価
- ON性能評価
- OFF性能評価
- ROC/AUC 評価
### 6.1 混同行列評価
事故データと通常走行データに対する判定結果から、TP、FP、FN、TN を集計し、基本指標を算出する。
### 6.2 ON性能評価
事故データを対象に、A2時間幅ごとに、初検知区間、シフトポジション、衝突までの時間および衝突速度低減効果を集計する。
### 6.3 OFF性能評価
通常走行データを対象に、誤検知回数、誤検知率、シフトポジションを集計する。
### 6.4 ROC/AUC 評価
事故データを正例、通常走行データを負例とし、ファイル単位スコアのしきい値を変化させたときの TPR と FPR を用いて ROC 曲線および AUC を算出する。
---
## 7. データ定義
本章は評価ロジックの前提となる必須定義を示す。
### 7.1 評価単位
基本の評価単位はファイル単位とする。
混同行列、基本指標、ROC、AUC はいずれもファイル件数ベースで算出する。
### 7.2 データ種別
評価対象となるファイルのデータ種別は、定義用ファイルにて定義されているものとする。
### 7.3 時系列情報
各ファイルは、少なくとも以下の時系列情報を持つものとする。
- 時刻情報
- モデル出力値または異常判定に必要な値
- シフトポジション
- アクセル開度
### 7.4 事故区間の扱い
事故データにおける区間の定義は、第 5 章の用語定義による。
事故データに対応する区間時刻情報は、少なくとも A1開始、A2開始、A3開始、A3終了を含むものとし、定義用ファイルにて定義されているものとする。
### 7.5 通常走行データの扱い
通常走行データは A1、A2、A3 による区間情報を持たず、区間判定の対象外とする。
### 7.6 判定結果への変換
評価にあたっては、モデルごとに異なる出力値を、各時点について「異常」または「正常」と判定可能な形で扱うものとする。
#### 7.6.1 Random Forest および Time Series 系モデル
各時点の予測ラベル、または予測値から導かれる判定結果を用いて、各時点を「異常」または「正常」に区分する。
#### 7.6.2 Autoencoder
各時点の再構成誤差を用い、所定の閾値を超えた時点を「異常」、閾値以下の時点を「正常」とする。
### 7.7 初検知時刻の扱い
初検知時刻は第 5 章の用語定義に従う。
一度も異常判定が存在しないファイルは、初検知時刻なしとして扱う。
### 7.8 初検知区間の扱い
初検知区間は第 5 章の用語定義に従う。
事故データにおける区間判定は以下のとおりとする。
- A1：初検知時刻が A1開始以上 A2開始未満である場合
- A2：初検知時刻が A2開始以上 A3開始未満である場合
- A3：初検知時刻が A3開始以上 A3終了以下である場合
- 初検知区間なし：上記のいずれにも属さない場合、または初検知時刻なしの場合
通常走行データについては初検知区間を初検知区間なしとする。
### 7.9 シフトポジション区分
シフトポジションは、車両がどの走行方向または状態で異常を初検知したかを区分して集計するため、以下の 3 つの状態で扱う。
- `D/B`：前進走行またはブレーキ状態
- `R`：後退走行
- `OTHER`：上記以外の状態
---
## 8. 混同行列評価仕様
### 8.1 目的
混同行列評価は以下の条件群で実施する。
- 条件A：A2 または A3 で異常を検知したものを検知ありとみなす条件
- 条件B：A2 で異常を検知したものを検知ありとみなす条件
なお、初検知区間の集計自体は A1、A2、A3 のいずれも保持する。
### 8.2 用語
| 用語 | 定義                                                                                                                  |
|------|-----------------------------------------------------------------------------------------------------------------------|
| TP   | 事故データについて、評価条件に適合する区間で異常が検知されたファイルをいう。                                         |
| FN   | 事故データについて、一度も異常を検知しないファイル、評価条件に適合しない区間のみで異常を検知するファイル、または初検知区間が初検知区間なしであるファイルをいう。 |
| TN   | 通常走行データについて、一度も異常を検知しないファイルをいう。                                                       |
| FP   | 通常走行データについて、一度でも異常を検知したファイルをいう。                                                       |
### 8.3 基本指標
基本指標は以下の式で算出する。
- `Accuracy = (TP + TN) / (TP + FP + FN + TN)`
- `Precision = TP / (TP + FP)`
- `Recall = TP / (TP + FN)`
- `TPR = TP / (TP + FN)`
- `FPR = FP / (FP + TN)`
- `FNR = FN / (TP + FN)`
- `TNR = TN / (FP + TN)`
---

## 9. ON性能評価仕様
### 9.1 目的
ON性能評価では、事故データに対して、異常をどの時点で検知できたかを評価する。
本章では、性能評価の指標を整理する。

### 9.2 用語
| 用語 | 定義 |
|---|---|
| ON性能 | 事故データに対する異常検知性能をいう。 |
| 衝突前誤踏み区間 | A2開始から A3開始までの時間をいう。 |
| 初検知から衝突までの時間 | 初検知区間が A1 または A2 の場合に、初検知時刻から衝突開始までの時間をいう。算出式は「A3開始時刻 - 初検知時刻」とする。 |
| 初検知から衝突までの時間割合 | A2開始から A3開始までの時間に対して、衝突までの時間が占める割合をいう。算出式は「衝突までの時間 / (A3開始時刻 - A2開始時刻)」とする。 |
| 衝突速度| A3開始時刻における車速をいう。本評価では、A3開始時刻と同一の時刻スタンプに記録された車速値を用いる。 |
| 仮想衝突速度| 初検知区間が A1 または A2 である事故データについて、初検知時刻における車速を、その後 A3開始時刻まで等速で維持したと仮定した場合の衝突時車速をいう。本評価では、ドライバーは加速抑制時にブレーキ操作を実施しないものとし、走行抵抗等による減速は考慮しない。 |
| 衝突速度低減量 | 衝突速度と仮想衝突速度の差をいう。負の値になる場合は 0 とする。 |
| 衝突速度低減割合 | 衝突速度に対して、衝突速度低減量がどの程度の割合を占めるかを示す値をいう。衝突速度が 0 の場合は算出対象外とする。 |

### 9.3 算出対象
集計対象は、以下の項目とする

#### 9.3.1 ファイル単位算出
各事故ファイルについて、以下を算出する。

| 算出項目 | 算出内容 | 算出対象 |
|---|---|---|
| 初検知区間 | 初検知時刻が A1、A2、A3 のいずれに属するかを判定する。未検知または区間外の場合は初検知区間なしとする。 | 全事故ファイル |
| 初検知時シフトポジション | 初検知時刻におけるシフトポジションを、前進走行またはブレーキ状態、後退走行、その他の状態に分類する。 | 初検知が存在する事故ファイル |
| 初検知から衝突までの時間 | 初検知時刻 - A3開始時刻 | 初検知区間が A1 または A2 の事故ファイル |
| 初検知から衝突までの時間割合 | 衝突までの時間/(A3開始 - A2開始) | 初検知区間が A1 または A2 の事故ファイル |
| 衝突速度 | A3開始時刻における車速を取得する。 | 初検知区間が A1 または A2 の事故ファイル |
| 仮想衝突速度 | 初検知時刻における車速を取得し、等速で衝突時刻まで進んだ場合の車速とみなす。 | 初検知区間が A1 または A2 の事故ファイル |
| 衝突速度低減量 | 衝突速度 - 仮想衝突速度。負の値になる場合は 0 とする。 | 初検知区間が A1 または A2 の事故ファイル |
| 衝突速度低減割合 | 衝突速度低減量/衝突速度を算出する。 | 初検知区間が A1 または A2 であり、衝突速度が 0 ではない事故ファイル |

次の条件に該当する場合、該当する算出項目は算出対象外とする。

| 条件 | 対象外となる項目 |
|---|---|
| 初検知区間が A3 または初検知区間なしである場合 | 衝突までの時間、衝突までの時間割合、衝突速度低減量、衝突速度低減割合 |
| A3開始時刻における車速が取得できない場合 | 衝突速度、衝突速度低減量、衝突速度低減割合 |
| 初検知時刻における車速が取得できない場合 | 仮想衝突速度、衝突速度低減量、衝突速度低減割合 |
| 衝突速度が 0 の場合 | 衝突速度低減割合。ただし、衝突速度低減量は算出可能とする。 |

### 9.4 A2時間幅ごとの集計
9.3.1で算出した各事故ファイルについてA2時間幅で以下の 3 区分に分類する。

| 区分 | 条件 |
|---|---|
| 短時間 | 0秒以上 3秒未満 |
| 中時間 | 3秒以上 5秒未満 |
| 長時間 | 5秒以上 |

## 10. OFF性能評価仕様
### 10.1 目的
OFF性能評価では、通常走行データに対して、誤検知が発生しなかったかを評価する。
本章では、性能評価の指標を整理する。
### 10.2 用語
| 用語     | 定義                                   |
|----------|----------------------------------------|
| OFF性能 | 通常走行データに対する誤検知性能をいう。 |

### 10.3 算出対象
集計対象は、以下の項目とする

#### 10.3.1 ファイル単位算出
各事故ファイルについて、以下を算出する。
| 算出項目 | 算出内容 | 算出対象 |
|---|---|---|
| 誤検知回数| 通常走行ファイルのうち、一度でも異常と判定されたファイル数を誤検知回数とする。| 全通常走行ファイル |
| 誤検知率 | ファイル数全体における誤検知率 |誤検知ファイル数 / 全ファイル数 | 全通常走行ファイル |
| 初検知時シフトポジション | 初検知時刻におけるシフトポジションを、前進走行またはブレーキ状態、後退走行、その他の状態に分類する。 | 初検知が存在する通常走行ファイル |

## 11 グループごとの集計
9章、10章の算出結果をもとに下記に記載の組合せごとにグループ化する。
グループ内容が定義されていない場合は無視する

### 11.1 グループ内容
下記に集計結果をグループ化する条件を記載する。
#### 11.1.1 アクセル開度グループ
アクセル開度は以下の 4 区分で評価する。
アクセル開度グループは排他的に分割せず、重複を許容する。

| 区分 | 条件 |
|---|---|
| 全開度範囲 | 0以上 100以下 |
| 70以上範囲 | 70以上 100以下 |
| 80以上範囲 | 80以上 100以下 |
| 90以上範囲 | 90以上 100以下 |

#### 11.1.2 グループ2

### 11.2 グループ数
11.1で定義したグループの掛け合わせ分のグループ数となる

### 11.3 グループ別集計
11.3 で定義した全 グループごとに、以下のファイル単位算出結果を集計する。

#### 11.3.1 ON性能評価
| 集計項目 | 集計内容 | 集計対象 |
|---|---|---|
| 初検知区間件数 | 全体検知数、A1件数、A2件数、A3件数、初検知区間なし件数を集計する。 | グループ条件を満たす全事故ファイル |
| 初検知時シフトポジション件数 | 前進走行またはブレーキ状態、後退走行、その他の状態の件数を集計する。 | グループ条件を満たし、かつ初検知が存在する事故ファイル |
| 初検知から衝突までの時間 | A1初検知群および A2初検知群について、衝突までの時間の平均および分散を算出する。 | グループ条件を満たし、かつ初検知区間が A1 または A2 の事故ファイル |
| 初検知から衝突までの時間割合 | A1初検知群および A2初検知群について、衝突までの時間割合の平均および分散を算出する。 | グループ条件を満たし、かつ初検知区間が A1 または A2 の事故ファイル |
| 衝突速度低減割合 | 衝突速度低減割合について、それぞれ平均および分散を算出する。 | グループ条件を満たし、かつ衝突速度低減効果のファイル単位算出条件を満たす事故ファイル |

#### 11.3.2 OFF性能評価
| 誤検知回数| 通常走行ファイルのうち、一度でも異常と判定されたファイル数を誤検知回数とする。| グループ条件を満たす全通常走行ファイル |
| 誤検知率 | ファイル数全体における誤検知率 |誤検知ファイル数 / 全ファイル数 | グループ条件を満たす全通常走行ファイル |
| 初検知時シフトポジション | 初検知時刻におけるシフトポジションを、前進走行またはブレーキ状態、後退走行、その他の状態に分類する。 |　グループ条件を満たし、かつ初検知が存在する通常走行ファイル |

## 12. ROC/AUC 評価仕様
### 12.1 目的
- 正例：事故データ
- 負例：通常走行データ
### 12.2 用語
| 用語                 | 定義                                                                                                                                                                                                                     |
|----------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 各時刻の異常スコア       | 各時刻における異常らしさを表す連続値をいう。値が大きいほど異常である可能性が高いことを示すものとする。Random Forest および Time Series 系モデルにおいては、各時刻における異常クラスの予測確率または異常側の判定スコアを各時刻の異常スコアとする。Autoencoder においては、各時刻における再構成誤差を各時刻の異常スコアとする。 |
| ROC評価に用いるスコア | 各時刻の異常スコアをファイル単位に集約した連続値をいう。ファイル単位スコアは、各ファイルにおける各時刻の異常スコアの上位 k% 平均異常スコアとする。                                                                                                               |
| AUC                  | ROC 曲線の下側面積をいう。事故データと通常走行データとの分離性能を表す指標であり、値が大きいほど分離性能が高いことを示すものとする。                                                                                                                       |
### 12.3 ROC 曲線の作成方法
各ファイルに対して算出したファイル単位スコアに複数のしきい値を適用し、しきい値ごとにファイル単位で TP、FP、FN、TN を求める。
その結果として得られる TPR および FPR を用いて ROC 曲線を作成する。
### 12.4 軸定義
ROC 曲線の軸は以下とする。
- 縦軸：TPR
- 横軸：FPR
横軸の表示は対数目盛を用いるものとし、代表点は `10^-4`、`10^-3`、`10^-2`、`10^-1`、`10^0` とする。
表示範囲外となる点、または表現不能な点は描画対象外とする。
### 12.5 算出対象
以下を算出対象とする。
- ROC 曲線
- AUC
---

## 13. 出力対象
　最終的に出力するデータ一覧を記載する。
　下記の内容が出力されるスクリプトとなっているかチェックする項目として使用する。
### 13.1 混同行列評価の出力対象
下記データは8.1で定義した各条件ごとの結果を出力していること。
- TP
- FP
- FN
- TN
- TPR
- FPR
- FNR
- TNR
- Confusion Matrix
- Accuracy
- Precision
- Recall

### 13.2 ON性能評価の出力対象
下記データは9.3.1、9.4で定義している内容結果をファイルごとに出力していること
- 初検知区間
- 初検知時シフトポジション
- 衝突までの時間
- 衝突までの時間割合
- 衝突速度
- 仮想衝突速度
- 衝突速度低減量
- 衝突速度低減割合
- A2時間幅ごとのグループ区分


### 13.3 OFF性能評価の出力対象
下記データは9.3.1、9.4で定義している内容結果をファイルごとに出力していること
- 誤検知回数
- 誤検知率
- 初検知時シフトポジション

### 13.4グループ条件ごとの算出結果
#### 13.4.1 ON性能評価

- 初検知区間件数 
- 初検知時シフトポジション件数
- 初検知から衝突までの時間
- 初検知から衝突までの時間割合
- 衝突速度低減割合

#### 13.4.2 OFF性能評価
- 誤検知回数
- 誤検知率
- 初検知時シフトポジション

### 13.5 ROC/AUC 評価仕様
- ROC 曲線のグラフ
- AUC